{
  "name": "leadsprint",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "./scripts/dev.sh",
    "web:dev": "cd apps/web && npm run dev",
    "web:build": "cd apps/web && npm run build",
    "web:start": "cd apps/web && npm run start",
    "api:dev": "cd apps/api && npm run dev",
    "api:test": "cd apps/api && npm test",
    "api:db:init": "cd apps/api && npm run db:init",
    "api:db:seed": "cd apps/api && npm run db:seed"
  }
}
