# Optional API defaults for temporary deployments
DEFAULT_ORG_ID=org_default
DEFAULT_ORG_NAME=LeadSprint Demo Org
APP_BASE_URL=http://127.0.0.1:4000

# Shared secret for signed server-to-server auth requests from web -> api
INTERNAL_API_AUTH_SECRET=

# Gmail OAuth JSON may also be provided via file path if/when used
# GMAIL_JSON_PATH=/absolute/path/to/client_secret.json
